# Dependencies
node_modules/
*/node_modules/ # To be thorough for any nested node_modules outside workspace packages

# Next.js build outputs and cache
.next/
out/

# TypeScript build outputs (if any are not in node_modules or .next)
dist/ # If your 'api' or 'shared-types' build to a 'dist' you don't want to commit
      # However, for shared-types, you *might* commit 'dist' if other projects
      # consume it directly without a build step in CI. For V1, ignoring is safer.

# Operating System files
.DS_Store
Thumbs.db

# Environment files
.env
.env*.local
!.env.example # Important: Keep example env files

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor/IDE specific folders
.vscode/
.idea/

# Turbopack
.turbo/

# Serwist
public/sw*
public/swe-worker*
